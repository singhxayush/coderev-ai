{
  "name": "output",
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --port 8001",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "build": "echo 'no build step'"
  }
}
